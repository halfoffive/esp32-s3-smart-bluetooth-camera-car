# App Build - Flutter + Rust 跨端 App CI 流水线
# 详见 spec: .trae/specs/smart-bt-camera-car/spec.md
#
# 流水线职责：
# 1. cargo doc 生成 Rust 子 crate 文档（独立 job，先跑）
# 2. 矩阵构建 Android APK + Linux/Windows/macOS 桌面包
# 3. tag 推送时聚合各 artifact 创建 GitHub Release
#
# 构建说明见 app/README.md

name: App Build

on:
  push:
    branches: [main, master]
    tags: ['v*']
    paths:
      - 'app/**'
      - '.github/workflows/app.yml'
  pull_request:
    paths:
      - 'app/**'
      - '.github/workflows/app.yml'
  workflow_dispatch:

jobs:
  # Job 1: Rust 文档生成（无业务依赖，快速暴露接口问题）
  cargo-doc:
    runs-on: ubuntu-latest
    steps:
      # 1. 检出仓库代码
      - name: Checkout
        uses: actions/checkout@v7.0.0

      # 2. 配置 Flutter stable 通道（生成绑定需要）
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      # 3. 配置 Rust stable 工具链
      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      # 4. 安装 flutter_rust_bridge_codegen（cargo 官方推荐安装方式）
      # app/rust/src/frb_generated.rs 与 app/lib/src/rust/ 均为 gitignored，
      # 必须在 CI 中重新生成，否则 lib.rs 的 `mod frb_generated;` 会导致 E0583。
      - name: Install flutter_rust_bridge_codegen
        run: cargo install flutter_rust_bridge_codegen

      # 5. 预装 cargo-expand（flutter_rust_bridge_codegen generate 的传递依赖），
      #    提前装好可让 generate 步骤跳过传递编译/下载，显著加速 codegen。
      - name: Install cargo-expand
        run: cargo install cargo-expand

      # 6. 清理平台目录，避免残留文件导致 flutter create . 识别为已有目录而跳过生成
      - name: Clean platform directories
        shell: bash
        run: rm -rf app/android app/ios app/linux app/macos app/windows

      # 7. 补全 Flutter 平台原生目录（已存在则无副作用，不覆盖 lib/ 与 pubspec.yaml）
      - name: Bootstrap Flutter platforms
        run: cd app && flutter create . --platforms=android,linux,windows,macos --org com.smartcar --project-name smart_car_remote

      # 8. 生成 Rust ↔ Dart 绑定代码（产出 frb_generated.rs）
      - name: Generate flutter_rust_bridge bindings
        run: cd app && flutter_rust_bridge_codegen generate

      # 9. 生成 Rust 子 crate 文档（不编译依赖、开启全部 feature，要求无警告）
      - name: Generate cargo doc
        run: cd app/rust && cargo doc --no-deps --all-features

      # 10. 上传文档产物
      - name: Upload cargo doc
        uses: actions/upload-artifact@v7.0.1
        with:
          name: cargo-doc
          path: app/rust/target/doc

  # Job 2: 跨平台构建矩阵（Android APK + Linux/Windows/macOS 桌面）
  build-matrix:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            flutter_target: apk
            rust_target: aarch64-linux-android
          - os: ubuntu-latest
            flutter_target: linux
            rust_target: x86_64-unknown-linux-gnu
          - os: windows-latest
            flutter_target: windows
            rust_target: x86_64-pc-windows-msvc
          - os: macos-latest
            flutter_target: macos
            rust_target: x86_64-apple-darwin

    steps:
      # 1. 检出仓库代码
      - name: Checkout
        uses: actions/checkout@v7.0.0

      # 2. 配置 Flutter stable 通道
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      # 3. 配置 Rust stable 工具链
      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      # 4. 安装 flutter_rust_bridge_codegen（cargo 官方推荐安装方式）
      - name: Install flutter_rust_bridge_codegen
        run: cargo install flutter_rust_bridge_codegen

      # 5. 预装 cargo-expand（flutter_rust_bridge_codegen generate 的传递依赖），
      #    提前装好可让 generate 步骤跳过传递编译/下载，显著加速 codegen。
      - name: Install cargo-expand
        run: cargo install cargo-expand

      # 6. 清理平台目录，避免残留文件导致 flutter create . 识别为已有目录而跳过生成
      - name: Clean platform directories
        shell: bash
        run: rm -rf app/android app/ios app/linux app/macos app/windows

      # 7. 补全 Flutter 平台原生目录（已存在则无副作用，不覆盖 lib/ 与 pubspec.yaml）
      - name: Bootstrap Flutter platforms
        run: cd app && flutter create . --platforms=android,linux,windows,macos --org com.smartcar --project-name smart_car_remote

      # 8. patch Android compileSdk 至 35（仅 Android 构建需要）：flutter_reactive_ble 依赖 AndroidX 1.7.x
      #      要求 compileSdk ≥ 34；android/ 由 flutter create 生成、不在版本控制中，须在 CI 内 patch
      - name: Patch Android compileSdk
        if: matrix.flutter_target == 'apk'
        run: |
          test -f app/android/app/build.gradle || { echo "ERROR: app/android/app/build.gradle not found"; exit 1; }
          sed -i 's/compileSdk = [0-9]*/compileSdk = 35/' app/android/app/build.gradle
          # 在根 build.gradle 注入 subprojects 块，强制所有插件模块（含 reactive_ble_mobile）compileSdk 35
          cat >> app/android/build.gradle <<'EOF'

          subprojects {
              afterEvaluate { project ->
                  if (project.hasProperty("android")) {
                      project.android {
                          compileSdk 35
                      }
                  }
              }
          }
          EOF

      # 9. 生成 Rust ↔ Dart 绑定代码
      - name: Generate flutter_rust_bridge bindings
        run: cd app && flutter_rust_bridge_codegen generate

      # 10. 安装 Linux 桌面构建依赖（仅 ubuntu runner 需要，apk/linux 共用）
      - name: Install Linux desktop dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build libgtk-3-dev clang cmake

      # 11. 添加 Rust 交叉编译目标（按矩阵条目）
      - name: Add Rust target
        run: rustup target add ${{ matrix.rust_target }}

      # 12. 拉取 Dart 依赖
      - name: Flutter pub get
        run: cd app && flutter pub get

      # 13. 按平台分支构建 release 产物
      - name: Build Android APK
        if: matrix.flutter_target == 'apk'
        run: cd app && flutter build apk --release

      - name: Build Linux desktop
        if: matrix.flutter_target == 'linux'
        run: cd app && flutter build linux --release

      - name: Build Windows desktop
        if: matrix.flutter_target == 'windows'
        run: cd app && flutter build windows --release

      - name: Build macOS desktop
        if: matrix.flutter_target == 'macos'
        run: cd app && flutter build macos --release

      # 14. 上传构建产物（按平台区分命名）
      - name: Upload Android artifact
        if: matrix.flutter_target == 'apk'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: app-android
          path: app/build/app/outputs/flutter-apk/app-release.apk

      - name: Upload Linux artifact
        if: matrix.flutter_target == 'linux'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: app-linux
          path: app/build/linux/x64/release/bundle/*

      - name: Upload Windows artifact
        if: matrix.flutter_target == 'windows'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: app-windows
          path: app/build/windows/x64/runner/Release/*

      - name: Upload macOS artifact
        if: matrix.flutter_target == 'macos'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: app-macos
          path: app/build/macos/Build/Products/Release/*.app

  # Job 3: tag 推送时聚合各 artifact 创建 GitHub Release（与 firmware.yml 风格一致）
  release:
    needs: [cargo-doc, build-matrix]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0

      # 下载所有构建产物到 artifacts/ 目录（每个 artifact 一个子目录）
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      # 创建 Release 并附带各平台二进制
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/app-android/*
            artifacts/app-linux/*
            artifacts/app-windows/*
            artifacts/app-macos/*
